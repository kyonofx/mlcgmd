from setuptools import setup, find_packages

setup(
    name="graphwm",
    version="1.0.0",
    description='Simulate time-integrated coarse-grained MD with graph neural network world models (graphwm)',
    packages=find_packages(),
    install_requires=[
    ]
)